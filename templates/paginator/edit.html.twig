{% extends 'base.html.twig' %}

{% block title %}Edit Paginator{% endblock %}

{% block body %}
    <h1>Edit Paginator</h1>

    {{ include('paginator/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('paginator_index') }}">back to list</a>

    {{ include('paginator/_delete_form.html.twig') }}
{% endblock %}
