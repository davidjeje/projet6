<form method="post" action="{{ path('tricks_delete_get', {'id': trick.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette figure ?');">    
    <i class="fas fa-trash"></i>
</form>
