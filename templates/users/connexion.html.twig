{% extends 'base.html.twig' %}
{% block slide1 %}
<div class="col-md-6">
				 <div class="login-page">
					{{form(form)}}
				  </div>
				  <br>
				  <button> <a href="{{ path('forgot')}}">Cliquer ici si vous avez oublier votre mot de passe</a></button>
</div>
				<div class="col-md-6">
				 	<div class="login-title">

					</div>
			     </div>
		{% block slide3 %}
        
        {% endblock %}		 
			 
	  {% block container %}
	  {% if error %}
        <div class="alert alert-danger">
            <ul class="list-unstyled mb-0">
                <li><span class="initialism form-error-icon badge badge-danger">Error</span> 
                    <span class="form-error-message">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                </li>
            </ul>
        </div>
    {% endif %}
    {{form(form)}}
    {% endblock %}
{% endblock %}