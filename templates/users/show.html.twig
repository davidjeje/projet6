{% extends 'base.html.twig' %}

{% block title %}Users{% endblock %}

{% block content1 %}
    <h1>Users</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>PlainPassword</th>
                <td>{{ user.plainPassword }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ user.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tricks_index') }}">Revenir à la liste des figures</a>

    <a href="{{ path('users_edit', {'id': user.id}) }}">Modifier</a>

    {{ include('users/_delete_form.html.twig') }}
{% endblock %}
